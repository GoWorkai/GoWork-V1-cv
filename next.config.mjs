/** @type {import('next').NextConfig} */
const nextConfig = {
  // Forzar regeneración de páginas estáticas
  experimental: {
    staleTimes: {
      dynamic: 0,
      static: 0,
    },
  },
  // Headers para evitar caché
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          {
            key: 'Cache-Control',
            value: 'no-cache, no-store, must-revalidate',
          },
          {
            key: 'Pragma',
            value: 'no-cache',
          },
          {
            key: 'Expires',
            value: '0',
          },
        ],
      },
    ]
  },
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
}

export default nextConfig
